
// Scratch pad

const styler = (obj) => {
  // Pass in style object
  // - create hashed className
  // - add to stylesheet
  //
  // returns hook for className
}

/*

const cx = styler({
  root: {
    margin: 8,
    padding: 8
  }
})

<div className={cx.root}>
</div>



*/


