
import Color from 'color'

export const alpha = (b, a) => Color(b).alpha(a).rgbString()
export const darken = (b, n) => Color(b).darken(n).rgbString()

export const blue = '#07c'
export const red = '#f02'

